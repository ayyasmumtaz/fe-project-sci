// Central registry of route paths. Add one key per page/feature you build,
// then reference ROUTE_PATHS.xxx everywhere instead of hardcoding strings.
export const ROUTE_PATHS = {
  // example: dashboard: '/dashboard',
} as const;
